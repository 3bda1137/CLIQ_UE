using Microsoft.AspNetCore.SignalR;

namespace CLIQ_UE.Hubs
{
    public class NotificationHub : Hub
    {

        //await Clients.User(userId).SendAsync("ReceiveFollowNotification");



        //await Clients.User(userId).SendAsync("ReceiveUnfollowNotification");


        //await Clients.User(userId).SendAsync("CommentNotification", postId, userId);
        //notificationService.AddNotification(followingId, currentUser.Id, "commented on your post");

        //await Clients.User(userId).SendAsync("LikeNotification", postId, userId);
        //notificationService.AddNotification(followingId, currentUser.Id, "loved your post");

        //await Clients.User(userId).SendAsync("DisLikeNotification", postId, userId);
        //notificationService.AddNotification(followingId, currentUser.Id, "disliked your post");


        //await Clients.User(userId).SendAsync("NewMessageNotification", postId, userId);
        //notificationService.AddNotification(followingId, currentUser.Id, "sent you a message");


    }
}
